import { AppPage } from '@/pages/shared/AppPage';

export default function BillingPage() {
  return (
    <AppPage
      title="Billing"
      description="Subscriptions and invoices — administrators only."
    />
  );
}
